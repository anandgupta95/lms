package com.lms.repository;

import com.lms.model.EduPoint;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EduPointRepository extends JpaRepository<EduPoint,Long> {
}
